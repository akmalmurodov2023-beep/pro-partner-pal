# Client-side (exposed to browser, prefixed with VITE_)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# NOTE: Server secrets are NOT placed here.
# They are stored as Supabase Edge Function secrets:
#   supabase secrets set TELEGRAM_BOT_TOKEN=xxx
#   supabase secrets set GOOGLE_DRIVE_API_KEY=xxx
#   supabase secrets set LOVABLE_API_KEY=xxx
